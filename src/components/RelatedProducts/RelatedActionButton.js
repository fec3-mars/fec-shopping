import React from 'react';

//this action button is shaped like a star
//it's part of each related products card. onClick it should display a comparison diagram of clickec target vs current product

class RelatedActionButton extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return(
      <div>
        <button></button>
      </div>
    )
  }
}

export default RelatedActionButton;