import React from 'react';

//this addoutfitButton is by default under Your Outfit
//it stays at left most in the list. when it's clicked, it adds the current product into outfit list
//makes sure that one product can only be added once

class AddOutfitButton extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return(
      <div>
        <button></button>
      </div>
    )
  }
}

export default AddOutfitButton;